import 'package:flutter/material.dart';

class AdminAux2Screen extends StatelessWidget {
  const AdminAux2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Admin aux2'),
    );
  }
}
