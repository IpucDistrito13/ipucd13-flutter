import 'package:flutter/material.dart';

class SuperAux1Screen extends StatelessWidget {
  const SuperAux1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Super aux1'),
    );
  }
}
